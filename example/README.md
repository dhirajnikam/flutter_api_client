# Flutter API Client Example

This example demonstrates how to use the `flutter_api_client` package.

## Usage

```bash
cd example
flutter pub get
flutter run
```

The app fetches a sample post from JSONPlaceholder API when you tap the button.
