name: flutter_api_client_example
description: Example app for flutter_api_client package.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_api_client:
    path: ../

dev_dependencies:
  build_runner: ^2.4.0
  source_gen: ^1.5.0
